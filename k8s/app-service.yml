apiVersion: v1
kind: Service
metadata:
  name: mszlu-ai-app-service
  namespace: mszlu-ai
  labels:
    app: mszlu-ai-app
spec:
  selector:
    app: mszlu-ai-app
  ports:
    - name: http
      port: 80
      targetPort: 8888
      protocol: TCP
  type: ClusterIP